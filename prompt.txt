Go through errors.txt and see the errors.


see getProduct method in EcommerceCatalogController controller in errum_be.
currently it has a variable relatedProducts. This provides the data for related products in e-commerce/product/[id] page.tsx



Go through all the migrations, models and controllers in errum_be that are related to product, product_batches, product_variants, inventory and store and understand their functionalities properly.
What i want you to find out, there should be mention of a physical store/branch where products are stored (not warehouse but the shoe shop branches).
The inventories/product_batches for a product (product not sku, meaning name-color-size-36, instead of name) also have the information for which store has how many pieces of that product variant.

What i want:
->make a new method in EcommerceCatalogController named getBranchStockForProduct
->it should query/map how many count of a particular product (a variant, not a sku) is present in each stores. 
->for each store, it will return the id, name (if present) and the inventory count for that product id.
->add the route to the api.php and make that route public.

The change in frontend:
->When the e-commerce/product/[id]/page.tsx is called, it should call this as well.
->it will show a beautiful, stylish, modern table consistent with the theme of the website below the "categories-availability" card and above the "free deliveries" "Easy return" cards.
->If, a branch doesn't have that product, it should not be on the table, basically, an intelligent table.
->check the services, lib files to know where to add the api calls.

Make sure there's no code integrity issues and do not change any additional UI or logic issues.
make a detailed md file titled "added Branch inventory to e-commerce/product/id page." It should discuss and explain in detail how the new pages and controller fucntions, examples and edge cases.



see app/inventory/outlet-stock page.tsx. Check which routes in the backend it's calling to fetch outlet-stock information.
go to errum_be and read all those controllers, their models and all the migration files related to them.

then remove getBranchStockForProduct method EcommerceCatalogController.
update e-commerce/product/[id] page.tsx to stop using getBranchStockForProduct and instead get outlet_stock information from the same api_roots as outlet-stock page.tsx
if those routes were previously protected, make them public in both frontend and backend.
these routes are used by other pages as well, so make sure that making them public doesn't cause issues, like not knowing user through authentication.

try to make as minimal change as possible and make the UI, logic consistent with the rest of the site.
DO NOT change any other code. ONLY change the stricly necessary code.

see all controller, models and migrations for product product_variants, product_field, categories


go through all the files in e-commerce folder and all the components they use.
understand the UI theme and design preferences.

Then edit the mobile viewport support for all those pages.

What I want you to do for additional mobile viewports:
->Make the pages look properly structured and proportioned
->Make the pop-up modal and slide in modals animation smoother
->add a coherent seamless flow for the mobile viewport. 
->Make it look very polished

What i do not want you to do:
->DO NOT change or remove any logic or any additional UI currrently in use
->ONLY change or update ui for mobile viewport.
->You don't have to Update any code outside of styling in the pages and their components.

make a detailed md file titled "mobile viewport polishing for e-commerce pages" It should discuss and explain in detail how the new pages behave, examples and edge cases.

There may be some partially done works from previous prompts. edit from whatever condition you find.

Check the product/list page.
It has a search bar, filter and min-max input fields. I want you to check and do following changes:

->currently, the searchBar is having issues finding the product when we input the full name. 
->The filters and min-max input fields are all working fine. Make a small edit in frontend that, if min can not be larger than max and max can not be smaller than min (if inputed, change to equal) (but, only one can also be sent).

check the backend controllers and models that are called by these frontend code. make sure that the entire data fetching and request sending is seamless and smooth.
The page is currently working absolutely fine with proper logic. so ONLY edit if stricly necessary. DO NOT change anything unnecessarily. 



check the pages in @app/ that are relevant to product, product_batches, product_variants, product_field and product_images.
Also view all the code in components, services and lib folders to understand anything relevant.
/compress

### retur-refund:
Analyze the @errum_be/app/Http/Controllers, @errum_be/app/Models, @errum_be/app/Traits/ProductImageFallback.php
and the migrations in @errum_be/database/migrations.

understand the product and order return, refund, exchange logic.

Check the code for integrity issues, potential bugs, risks and edge cases along with partial payment additional, parital payment refund etc cases.

generate a detailed md file titled "25_Mar_26 product return, refund, exchange integrity check.md" with discussion and explanation in detail how the code behaves, examples and edge cases and mention the files in context.

### images:
Analyze the @errum_be/app/Http/Controllers, @errum_be/app/Models, @errum_be/app/Traits/ProductImageFallback.php
and the migrations in @errum_be/database/migrations.

understand the logic behind image store, show, get, update, delete etc...
currently, we have this issue:
some of the product images are available in the backend storage with symlink. we can see some images like this from frontend. But,
other images are not shown. inspecting the Networks tab shows those image requests returned a 404 not found.
(see GEMINI.md to get the backend API and test a few calls yourself. You would have to call the product to get it's images to actually call the image)
Here are some image urls that work (test them to see if you understand something):
Working:
https://backend2.errumbd.com/storage/products/4441/1772440412_OYlGLPqMUS.jpeg
https://backend2.errumbd.com/storage/products/4453/1772441132_XxQa5cJRyo.jpeg

Not Working:
https://backend2.errumbd.com/storage/products/5075/1773420180_G3RBY2VtuK.jpeg
https://backend2.errumbd.com/storage/products/5013/1773394108_0MFhrmFoCb.jpeg

we've manually checked to confirm these files exists under this exact name in those places.

Something you need to know:
->errum_be is only the code for the backend for the sake of prompting. so you won't find the symlink or the images there.

try and test different products to get product_images url and test if you want.
understand the issue and problem and propose a fix for it.

Write the proposal in a detailed md file titled: "25_Mar_26 404 image issue identification and solve proposal.md" and save it as an md file in @Documentation/
