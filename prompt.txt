see app/inventory/outlet-stock page.tsx. Check which routes in the backend it's calling to fetch outlet-stock information.
go to Deshio_be and read all those controllers, their models and all the migration files related to them.

then remove getBranchStockForProduct method EcommerceCatalogController.
update e-commerce/product/[id] page.tsx to stop using getBranchStockForProduct and instead get outlet_stock information from the same api_roots as outlet-stock page.tsx
if those routes were previously protected, make them public in both frontend and backend.
these routes are used by other pages as well, so make sure that making them public doesn't cause issues, like not knowing user through authentication.

try to make as minimal change as possible and make the UI, logic consistent with the rest of the site.
DO NOT change any other code. ONLY change the stricly necessary code.

see all controller, models and migrations for product product_variants, product_field, categories


go through all the files in e-commerce folder and all the components they use.
understand the UI theme and design preferences.

Then edit the mobile viewport support for all those pages.

What I want you to do for additional mobile viewports:
->Make the pages look properly structured and proportioned
->Make the pop-up modal and slide in modals animation smoother
->add a coherent seamless flow for the mobile viewport. 
->Make it look very polished

What i do not want you to do:
->DO NOT change or remove any logic or any additional UI currrently in use
->ONLY change or update ui for mobile viewport.
->You don't have to Update any code outside of styling in the pages and their components.

make a detailed md file titled "mobile viewport polishing for e-commerce pages" It should discuss and explain in detail how the new pages behave, examples and edge cases.

There may be some partially done works from previous prompts. edit from whatever condition you find.

Check the product/list page.
It has a search bar, filter and min-max input fields. I want you to check and do following changes:

->currently, the searchBar is having issues finding the product when we input the full name. 
->The filters and min-max input fields are all working fine. Make a small edit in frontend that, if min can not be larger than max and max can not be smaller than min (if inputed, change to equal) (but, only one can also be sent).

check the backend controllers and models that are called by these frontend code. make sure that the entire data fetching and request sending is seamless and smooth.
The page is currently working absolutely fine with proper logic. so ONLY edit if stricly necessary. DO NOT change anything unnecessarily. 



check the pages in @app/ that are relevant to product, product_batches, product_variants, product_field and product_images.
Also view all the code in components, services and lib folders to understand anything relevant.
/compress


Check social-commerce page. currently, when we search a product the following thing happens:
->the page most likely uses a very old backend code for search that takes a very long time to respond
->or it uses a new version, but not properly calls it.
->Shows all the batches in different branches.

What i want you to do:
->Check the e-commerce/search page and e-commerce/product/[id] page and see which backend code they call and how they're called
->then check their entire backend codes (all controllers and models)

Then update the social-commerce page for the following:
->The product search feature should use the same search method used by those pages.
->The results should also show within the product list the count of each product batch in the branch. We'll show an aggregate, for example, if there's two batches (qty 9, 10) in a branch, we'll show qty 19.

If the output of the confirm order was adding batch id to it, change it and instead send batch_id as null.

Make an absolutely detailed, very descriptive, lengthy md file titled "25_Mar_26 social commerce page Updates.md". It should discuss and explain in detail how the new page behave, examples and edge cases.
DO NOT change anything else. DO not make any chnages to backend. DO NOT change any code unless strictly necessary.

I ran a full analysis of the relevant codebase with Gemini CLI. This was the response:

### product search for social-commerce page:
check the social-commerce page. check all the backend code it calls.

currently, it's searchbar retrieves products like this:
->uses the same backend code as e-commerce pages and retrieves products grouped by their SKUs
->shows the aggregate of all batches of all product_variants in one store as qty in that store.

I want you to do the following:
->check all the backend controllers and models that are related to product, product_batch, product_field, product_variant to see if there's a method that sends back each individual product (variant with the base_name-color-field) and not the SKU
->If you find a suitable method that can be used to get single products, use that. Make sure the request frontend sends is the same as backend receives. The backend code MUST be backwards compatible.
->update the page so that, instead of showing store 1, store 2, etc, it shows the store name. see e-commerce/product/[id] page and its backend code and find out how to get the store names.

DO NOT change any additional code.
DO NOT change any backend code unless stricly necessary. ANY change in backend MUST be backwards compatible.
ANY change in frontend should be aware of the dark vs light theme.

update the frontend_Api_controller_mapping.md for this page (if any).
generate and save a detailed md file /storage/Fullstack-Laravel-Next/Deshio_v2/Documentation/27_Mar_26 reserve stock frontend plan.md It should discuss and explain any issue you find, make a proper workplan and describe in very detail how the new functionalities work.
I'll use this md file to tell Gemini in antigravity.


### Stock deduction issue solve:

read Documentation/26_Mar_26 reserve stock plan.md and understand the entire plan. I've also pasted it below:

Read the entire backend controller, model related to order, product, product_batch and anything relevant.

Implement the changes mentioned in the md file.
DO NOT make any additional changes. ONLY change the stricly necessary part.
then, make an absolutely detailed, very descriptive, lengthy md file titled "26_Mar_26 reserve stock.md". It should discuss and explain in detail how the new functionalities behave, examples and edge cases.

What you need to know:
->the files in Deshio_be is the backend code. 
->DO NOT run "php artisan make:something name" and instead directly create the file
->DO NOT run th migrations. 
->Just create the additional files and write the code.
->If the observers or anything similar need to be registered, register them (see the other observers to decide if that's necessary).                                                                             


### return and exchange issue: 

see full frontend and backend code to understand the process related to return and exchange.
firstly, change this: /lookup page will be available to everybody (all roles) and any call from here will not have store_id injected to them. We will skipStoreScope.

currently, we can initiate return and exchange from /purchase-history, /orders page, /lookup. see the logic in /purchase-history for return and exchange.
then, we want that same logic and UI in /lookup page. but, return-exchange can only be initiated by (admins) branch-manager and POS.
returns and exchanges initiated from here should be auto-approved like purchase-history page. 
the store selection in this page "/lookup" should also not force store selection in any dropdown.

we want the stock-increase logic for returns the same as dispatch (lookup dispatch logic in the codebase).
It should be that, the product (identified by its barcode) we will increase the stock of that product in the store where it was returned and copy the batch details from the store it was sold from.

the exchange logic should be like this:
->the product (identified by its barcode) we will increase the stock of that product in the store where it was returned and copy the batch details from the store it was sold from.
->then the customer needs to be refunded. and, refund can be none (for exact same price) or refund or pay more (for more payment take to POS page)

some or all of these features may have already been Implemented. but, it had issues like, when we returned a product, the stock was reduced (instead of increase) and two (the old one and a new item) became unvailable
also, exchange was working fine but created some availability issues here and there.

Since the system is in production, we can not afford to break anything. so, please be very careful. 
Since, testing confirmed that return and exchange was mostly working except few availability issues, avoid doing any drastic changes to the already partially working process.
identify the issues and only make strictly necessary changes.
make sure the reserved_product system is aware of the changes.


### notepad:
cli message: 
see all the backend and frontend code to understand all transactions.
 I want to describe to you our finance and accounting procedures or ideas that we are going to follow. Let's begin with us buying eight products for our business. So we will count it as eight product items debit and our cash is credit. Then we will sell two from them, then two inventory credit, and then our cash will be debit and our sales revenue will be credit. And our cost of goods supplied, COGS, will be debit because that was returned to us. And the sales revenue will be automatically calculated from the data and updated.  Now, for the second part.I'm gonna describe to you the return procedure. Let's say of the two sold products, when the returned one, then that one product item would be debited and the COGS will be credit, alongside the cash, and the sales revenue will be debit. This is the reversal or the reverse process for sale. For the last part, we have exchange, and exchange can take three scenarios.  So the first scenario would be the items exchanged have same price. In that case, cash has no change, and the net change would be the old product item will be debited back to inventory and the COGS will be credited. As for the new item that's been given, its inventory will be credited and the COGS would be debited. In a scenario where we are exchanging with a more expensive product, in that case for the old item, we're still going to debit the inventory, credit the COGS and cash, and the sales revenue will be debit. Now in this case, we do have cash change and revenue change because we are exchanging with a more pricier alternative and for the new product, which is being exchanged for, we'll have its inventory credited and COGS debited and the calculate net difference in that transaction, the net difference will be in cash, will be debit and the sales revenue will be credit. As for a scenario where we are exchanging with the old product, with a less expensive item, in this scenario, we are going to follow the same process for the old product as the logic for more pricier option. As for the new product item, the net difference in cash and sales revenue will be calculated and the cash will be credited and the revenue will be debited. 
then read Documentation/7_Apr_2026 example_accounting chart.md to understand example credit-debit scenarios.
we want individual topic wise ledgers like in (b) in those charts.

see account ledger in /accounting page. there's a dropdown in there for choosing which topic we want. From them, only "Cash And Cash Equivalents" is properly working (view it to understand how it tracks everything)
What i want you to do:
Read through everything and understand what needs to be done to track all transactions and sort them accordingly to the options in the account ledger dropdown.
Make a very detailed lengthy (300+ lines) md file for gemini in antigravity under Documentation/, titled "7_Apr_26_accounting_plan.md" explaining the entire workplan in full details, what needs to be done and which files will be affected. make it as detailed as possible.
DO NOT change any code. I want the detailed md file only.

these are the changes we want:
->all transactions should have their transaction id. some of them are having App/../.... etc.
->in manual entry, admins will have store selection dropdown, for branch manager it's preselected (unchangeable). branch should be optional. we also get to choose "Deshio" where, Deshio == whole company and note store specific expenses.
->BM will not be able to see/choose Deshio.
->manual entry allows one to add images and note/reference. it should be functional in both front and backend.
->transaction page transaction lists should not be grouped by types. instead, newest first (before pagination)
all these changes should be in sync with the accounting part of the site (see /accounting page and its backend code)

What i want you to do:
make me a Implementation plan md file that i can give to gemini in antigravity to make it work, without integrity issues and in an efficient way.
name the file like this: "7_Apr_26 ..." in Documentation/ folder.

I want you to go through my front-end and back-end and all the codes Regarding inventory pages, the inventory page is currently properly functional, so be very careful before editing it. We want these following changes, that the date filter is not working and it should be working. The date filter should control the data shown immediately beneath it. We are going to add date filters for the sales trends and then the pie chart shown below them, and then the branch performance. Basically, every single model will have its own date filter that should be responsive. The sales trend has its dates overlapping each other. We can remove the dates from the graph itself, but the graph should be highly customizable to set to yearly, monthly, weekly, daily within a time frame. The more necessary part is to show the curve, then the dates. The category are shown by the category ID. We need it to be category name. Also, the best seller products. These are not customized. These are the top products from all sales. We need the response to be customizable, meaning within this price range, within this category, this branch, within these categories, from these dates. And these queries should be nestable. In the branch performance, of course, we need the dates. In the stock watchlist, which shows the stock whose stock has gone below 5, all net stock has gone below 5, we want to show all such products in one fixed screen width and it should be scrollable with other details like how many days it has been in the store, basically the age of the batch and all that. The hourly order pulse is working, yes. We just want the UI to be more engaging instead of a whole hourly list. It could be like a clock or a pie chart or something similar.

see accounts.tsx. i want you to open the browser and login via /login using  super admin account. Check the transaction pages and accounting page. act as a professional tester. test all these pages. if you need to upload any image, upload this: public/e-commerce-hero.jpg . I want yout test and identify all the issue. then make a detailed md file in Documentation titled "8_Apr_26 ..." and log all the issues bugs,, edge cases there. DO NOT change any code now. i only want the md file.

### inventory reports:

check the inventory reports page and all its components. they are not properly working with the dark vs light theme.
some of the components' contents are now spilling out of their boxes like branch performance and best sellers. a few pixels short and they'd be fine.

the categories dropdown is not showing any categories or subcategories. check if it's not fetching properly or if it's a theme issue. categories and subcategories dropdown should be scrollable.

we want this new feature in inventory reports page:
->in first section and in sales trend, we need to add an option to see statistics for 1 single sku. add an input field for product sku, when given, we'll show results for that sku (seperate and unconnected btwn the one at upper section and the one in sales trend)

Since the system is in production, we can not afford to break anything. so, please be very careful. 
Since, testing confirmed that inventory reports was working and we want newer features, avoid doing any drastic changes to the already working process.


###  order export:

we want this new feature in orders page.
->when 1 or more order is selected we see these options: mark as delivered, pathao, print etc. we want a new one: export. use an excel or file icon.
->when we click on it, the we create a csv file exactly like order_sample.csv with same column names. the data should be for the selected orders only. and the file should be downloaded to the user's device.
->the csv file should be created in the backend and sent to the frontend for download.
->within those coloumns 'item type' will always be 'parcel', store name will be the name of the branch/store it is from, MerchantOrderId will be the order's unique order_id, recipient name, phone and address will be customer's name, phone, address. recipient city and zone and area if available otherwise empty, Amount to collect will be number (see pathao amount to collect), item quantity will be total number of items, item weight will alwats be 0.5 Kg, itemDesc will be a list of names of products ordered and special instruction if available otherwise empty. 
->in backend, simply make a new method in order controller to handle this.

we also want these features:
After a store has been assigned to a branch, we'll show name of the store under the order number in the order list.
there will be a store selection dropdown where we can select a store and see only orders assigned to that branch.

Since the system is in production, we can not afford to break anything. so, please be very careful.
Since, testing confirmed that order page was working and we want newer features, avoid doing any drastic changes to the already working process.


### inventory report page bug fix:
we added an skuperformancestudio component. it has made the global sku input section and the outputs are seperated. overhaul the page to smooth out these issues.
We no longer want individual sku inputs for different components. We'll only have one global sku input at the top (already working). and all components will show results for that sku. 

see product List page.
beside that sku input field, make a button that open a modal. in that modal,
we'll mimic the product list page, with all the filters and lists and search queries etc.
we'll show the base name here (and grouped sku wise) 
when we select a product from that modal, it'll be selected and the modal will close. and the sku input field will be updated with the selected product's sku. 

polish the page and proportion. currently, it looks squeezed and some components like branch performance is spilling out of its box.

### new barcode print for price adjustments
See inventory price adjustment page. we want to add a new feature there. 
->add a print button there. it will be clickable once a batch's price has been successfully changed. 
->when clicked, we'll print all barcodes of that batch which are 'available'.
->see components/GroupedAllBarcodesPrinter.tsx component and the bacodes printed should be exactly like that 

make the process efficient and without any integrity issues.
Since the system is in production, we can not afford to break anything. so, please be very careful. 
Since, testing confirmed that barcode printing and price-adjustment was totally working, avoid doing any drastic changes to the already working process.

### coupon features we want:
This is a description or a documentation of how campaigns and coupon generation is handled. Here you can see that campaigns can be generated in many forms like percentage of discount, fixed amount discount, buy one get one free, and then free shipping. Currently, we are ignoring buy one get one and free shipping. These are none of our concerns for now. In the percentage or an amount of,f campaign they can be both public and private, and more than likely, private campaigns do not have coupons. It's only reserved for private campaigns. Now, we want to make this whole campaigning system functional from our front end, which has yet not been fully functional, and also our new changes in the backend. Now, we are thinking of different scenarios of what can happen, and we need you to map it out for us.Now we are thinking of multiple scenarios of what can happen or what could happen. For example, a campaign can be run for only a few products and a percentage off from them. So then the productcard component should behave like that. Then there can be other scenarios where we are having an amount off, so that will only be shown in cart and checkout pages. Also, if it's a public, then that should be automatically added. If it's a private, then at the checkout page, we maybe have a coupon input field if we don't, then add one, and then we will deduct the amount for a private campaign. There can be other scenarios where we have a sale going on for some products and then they were added to a cart, and then we are adding a private campaign coupon and that should be functional. And all of this should be well noted. And I want you to map out all such possibilities of how this should behave like under certain circumstances, maybe 20-25 edge cases or different complex ideas, and I will use an AI model to implement out all those scenarios and make them functional.


### ui copy
all the existing text in app/e-commerce page is the source of truth. DO NOT, absolutely DO NOT change any of them. 
DO NOT change any text from any of the components they call.
by text, i mean any typed string inside <>text</> tags.

see frontend_blueprint.md . use this as an ideal frontend UI, styling and functionalities.
then, i want you update my e-commerce/ pages and the components they call to match that website
Use it as a source of inspiration and make the UI beautiful and modern.
absolutely DO NOT change/generate any text. all the pre-existing text MUST remain untouched.

update the e-commerce pages and its relevant components, global css etc. to match the blueprint.


### ecommerce better 

This is a blueprint of the e-commerce frontend side of our site.
We want to upgrade our UI.
 Our client's intent was like this: "We want our website to feel live. The fire emoji when stocks running out, even the fake "X people are viewing this product" all makes it better."

 On our clients theme, we want to both make the pages smoother and polished, but also make them livelier.
What I want you to do:
->generate ideas on how to make this e-commerce side look live and robust.

Give ideas for pages/components.
We don't have to force everything with live updates.
I want you to generate an md file with proposed changes along with their implementation details. 
If you want, we are considering to add a couple or two videos/short clips if necessary.

The e-commerce pages aren't mobile optimised (except find-stock, ignore that page for now), and mobile is our first priority, so we need to update each page for mobile optimisation anyways.
when you're building the suggestions, make each page (and or its components) a complete section. Like, i'll tell Ai to make changes in chunks instead of one go.



### adding insta reels:
in e-commerce home page, we have a section with these cards:
"sneaker heads", "Sweater Essentials" etc.
This also becomes list in mobile viewport.

We want to replace this section with this:
->a reel viewer, here there will be 5 reel videos, one in the center, other small paused videos on its left and right.
we can swipe to control which ones to see and small buttons too.
-> the center will be playing, keep sound at 60%. we'll add a small button bottom right to mute or unmute (dynamic).

the reel videos will be from here:
https://www.instagram.com/Deshio_bd/ . it has to be their latest 5 reels. checked in every 15 min or so. 
basically, when client uploads reels, they can see their e-commerce homepage get updated with the new reels. 
this has to be done in a way that doesn't break any existing functionality. you may need to add new components or modify existing ones. 


https://www.instagram.com/Deshio_bd/reel/DW6uSbkERA9/
https://www.instagram.com/Deshio_bd/p/DW6DQUGk-ho/
https://www.instagram.com/Deshio_bd/reel/DW323H0EboK/
https://www.instagram.com/Deshio_bd/p/DWwVRsnEzn8/
https://www.instagram.com/Deshio_bd/p/DWnvVqhE6-p/


### e-commerce fix:
Make the auto-slide infinite loop slide speed 65% of what it is right now.
The slide stops and starts on hover perfectly on desktop. but on mobile, it doesn't restart after tap. fix it. like, tap should go to that product image, but, like the touches that don't.

for each categories we are showing, we show all products, then each subcategories.
Update this.
For "All Products" under each category, we'll simply show all products of That category including subcategories of That category.


### variant selector
scenario wise issues:
["name": "Test-black-37", "variation_suffix": "-black-37",], ["name": "Test-white", "variation_suffix": "-white",] this shows: 36, Test-white while it should be: black-36, white 
["name": "Casual Popcorn Shirt Grey Marbel-na-l", "variation_suffix": "-na-l",] ["name": "Casual Popcorn Shirt Grey Marbel-na-xl", "variation_suffix": "-na-xl",] this perfectly shows: l, xl 
["name": "Imported Jacket - Ladies Lace Long Coat Brown-na-s", "variation_suffix": "-na-s",], ["name": "Imported Jacket - Ladies Lace Long Coat Brown-na-m", "variation_suffix": "-na-m",] this shows: Ladies Lace Long Coat Brown-s, Ladies Lace Long Coat Brown-m while it should be: s, m

Now, i can't iterate over all such edge cases and make special cases for each one. now naming hasn't been perfect in this site, but we need to make it work.
Our easiest chances is, use variation_suffix as source of truth (it is) and display accordingly. (the us7-EU41 logic is working fine).


### claude css doc gen

this is the frontend analysis ofthe ecommerce side now. i've added a json file that contains tokens of one the sites' design we wanted to copy.
Also, our client brand uses the colout cyan in their logo.

What i want you to do is:

generate a md file documenting the a colour pallete update for the entire e-commerce. We like off-white/ivory, cyan, gold etc. we are trying to avoid large sections of pitch black or dark grey.
i also want you to add design upgrades for all individual pages and components.
especially, premiumProductCard, it's widely used, but there's some design descrepencies accross the e-commerce pages. 

what we do not need to add or change: texts.
generate the md file such that, each section can be given to an ai model in chunks.
We want it to be aesthetically pleasing, smooth, and light-weight, but also have that "live" feeling.
the website already supports some very nice features, especially in homepage, so we don't necessarily want them gone.


#e-commerce copy

See New Ui folder. 

these are images of the website whose ui we want to copy in the e-commerce pages.

copy the ui, color schemes, and overall design.
the design should be focused on mobile view and optimised for that.

for the texts and descriptions, the original texts already present are the source of truth, do not change them anywhere at all.
the footer should have the texts as they are, simply update stylings.

basically, use the images as inspiration and update our current website to 90% similar to that.

this is what we want to keep from the old one: instagram viewer.

some pages like categoreies, search page, about-us etc are not in here. update all the pages, their components to match this new styling, theme and UI.


### sareng digital:
This is the e-commerce pages and components of a different project we made.
Simply use it to understand the pages we want. 

then make a detailed and lengthy md files for each page and component, explaining the changes we are going to make to make it Sareng Digital.

We want it feel modern, premium, smooth, and live. it should be dynamic and feel highly responsive.
especially make the home page compete with popular e-commerce sites like gadget and gear, startech and feel specially curated etc.

we want a hardcoded section for the Pendrives in the home page.
also, we want the hero image and insta reels to not be hardcoded. we want to be able to change them from admin panel. (something we'll do later. for now just keep it in mind)
for products, try to use pictures instead of Emojis.
try to avoid direct animal Emojis or pictures. we will prefer geometrics shapes/design or abstract icons.

This website should be highly optmised and built with mobile view as first priority while also catering to desktop view.

I want you to make an md file with the full instructions for an AI model to make these changes.


### making inventory view as xcel:

we want the inventory view page to behave like this:
currently, we have a dropdown view. we want to make it like an excel sheet.

we will show the existing datas in a meaningful excel format. an example has been given in picture.
we do not more coloumns than the data we are already showing. 
Since the system is in production, we can not afford to break anything. so, please be very careful. 
We do not want any change to the backend.


Currently, for 5 variation of 1 sku, we get 5 entries in the SKU col

oumn and product name coloumn. instead, for 1 sku, these rows should be merged to 1. the same for SKU total. (do this instead of hiding the value).


see /home/zunayed-mahmood/storage/Dev/deshio-front . this is an old version of this project.
there, look at product list page and its components.
there was a staging query option. when we opened "browse product list" from social-commerce page,
we used this feature.
we want this feature in our current product list page supported with social-commerce page.


see /home/zunayed-mahmood/storage/Dev/deshio-front . this is an old version of this project.
copy the POS receipt, barcode template and invoice from there to our current version.


i want you to check all the backend and frontend code and understand the workflow
see the discount related amount fields in payment amounts in POS, Social_commerce amounts detail, edit order 
check if all of them are working and there's no descrepencies.


### add "today" button on orders page.
the orders page already has filters for "social_commerce" "e-commerce" and all.
they are under a "more filters" dropdown.
instead, we want this "type:" filter like the top selections like "Markers" "Status".

we want a toggle button below that, that says "Today".
when on, it should filter the orders to only show today's orders.
make sure the orders returned (for all types of filtering and or querying) are filtered first then paginated instead of a filtering a paginated response.


###dispatch issue 

see the dispatch workflow and logic in storage/Dev/errum_v2/errum_be 

we are having issues with dispatch:
currently, scanning a barcode immediately reduces stock from the origin store.
cancelling a dispatch doesn't update stock

update the  logic so that stock is deducted only when dispatch is sent for approval.
cancelling or rejecting a dispatch should update the stock back.
the reserve_product table should also be updated accordingly.

a dispatch can be filled in, left like that, user comes back and scans some more products.
for these reasons, i want you to make an exact replica of the dispatch workflow created in storage/Dev/errum_v2/errum_be to here. 
update frontend accordingly.

### scm page image 
see the batch grid shown in social-commerce page and the backend code it calls  
currently, all batches are trying to show image, but since the api doesn't send any, we are shown the default image.

make a small tweak to add a thumbnail image for each batch, and display it in the batch grid. 
see product list page. it has a feature where hovering mouse over an image expands it.
we want that feature in the social-commerce page.


### order edit e rebalancing
currently, our system handles stock deduction for orders as reserve first.
any order through social_commerce or e-commerce gets first reserved.

we previously received a bug where multiple edit of same order resulted in a complete mismatch of stock across multiple batches of the product.
but, in the current system, we should not have that problem.

what i want you to do:
->act as a professional software engineer
->check the order edit and reserve stock logic thoroughly
->check the stock deduction logic thoroughly 

if there's a discrepancy, ideintify the issue and fix it.


### excel formatting

there's a feature in orders page to export to excel.
we want this additional features/changes in it:
->currently, it doesn't support bangla characters. all the names in bangla became "mojibake". fix it.
->the monile phone number doesn't get the leading 0. we must send it as number but have leading 0.
->there's a place for "Shop name". hardcode it as "Deshio BD - দেশীয়".


### barcode scan/lookup issue 

we found this bug:
when we scan a barcode for a product, let's say variant 95 of an sku, we are shown details of variant 97.
now, the identification that the barcode should belong to variant 95 is that, when it was printed, the full name was printed with the barcode and that said 95.
however, scaning it shows details of variant 97. 
we encountered this issue multiple times
(this doesn't imply that all barcodes printed for 95 showed 97 when scanned, only some of them)



this may be one of two issues:
either the barcode printing code has issues, or,
there has been some barcode mismatch accross batches.

i want you to act as a professional software engineer and codebase auditor. 
identify the issue and fix it.


### prod variation name issue 

We found this bug while creating a new variation under a sku.

example: a product (sku) is named "Jamdani Saree - JU1" then, it has varaitions like "DE1, DE2, MJ1, ..."
so, product names were like, "Jamdani Saree - JU1 - DE1", "Jamdani Saree - JU1 - DE2", "Jamdani Saree - JU1 - MJ1"
but, when we added a new variation, it became "Jamdani Saree - JU1 - DE1 - DE45". this  is an issue.

Also, let's say, we wanted to rename DE1 to DE2. Doing this changed all the names in the sku to "Jamdani Saree - JU1 - DE2". 
i want you to act as a professional software engineer and codebase auditor. 
identify the issue and fix it.


### barcode regeneration 

we found this issue:

when price is adjusted for a product variation,
new sets of barcodes are being generated for it. 
the logic should be that, no new barcodes are generated, but the price related information will be updated and when their barcodes are printed (made before or after the adustment, since barcode and price are independently under product) 
identify the issue and solve it. 

the selling price, cost price adjustment from inventory/batch-price-update page 
should cater batch-wisem, single changes.

i want you to act as a professional software engineer and codebase auditor. 
identify the issue and fix it.


### scm page image like JUY apu 

we want the image zoom in social commerce page to look like this:
remove the on hover effect.
make a modal that opens when user clicks on the image.
the modal shows the image(s) of the product in full size. (if possible, keep the current stock count )
hovering on the image zooms into the image (our goal is to see the exact pattern/design on the image)

also, update the order page. "today""social_commerce" and "pathao" marker should be preselected. we can change according to our will (but these three should be preselected by default)

### invoice for orders (add prod price)
 update the invoice print option for orders page. 
currently we are sending product details and qty. and then subtotal. we also want per piece price and total amount for that item. (piece price * qty = total amount for that item)
see the image to see the current format. 

### order edit after confirmation
currently we can edit orders in orders page as long as they are not confirmed (maybe. you need to check).
we want to be able to edit orders (remove products/services, add products/services, update qty) even after confirmation. most likely, products must be scanned before confirming.
so, when we add new products, only the additional needs scaning.
(this process should take barcodes-relabelling into consideration)
also, adding/removing qty (product) should rebalance stock and reserved_product table.


### order edit e advanced payment:
in social-commerce page, when we create an order, we can apply advanced payment (partial) on the order from (amount-details page).
the advance in order creation is saved (due shows perfectly) in orders page. 
but, when we edit the order, in amount details page, the partial advance is not tracked (we are shown full payment). and making advanced payment (new amount or old) is no longer saved.

we want the advanced, due, payment edit in order to be robust. 

### barcode relabelling:
We want this upgrade:
when we sell something through POS page in manual entry:
we want to do an automatic barcode relabelling in this manual entry process.
User searches products from manual entry, comes as individual products grouped under SKU.
we select one or more and (the rest of the logic stays the same).
But, sell the products does this:
(create a new controller to handle this inside a db:transaction:)
takes those products and calls the barcode relabelling methods inside.
it generates a(multiple) relabelled barcodes, assigned to product,then, we sell them (call same methods as we do in orderCreation for POS).
this process should handle addition of services to orders, however, they don't have barcodes to them.


###POS daily sales report 
completely rewrite the code for the pos daily sales report (that's exported).
it will show the store name, address and other store details.
then, it will  list all the  POS orders of that day.
For each order, it will show the order id, customer details.
then, it will show the order items,their barcodes, qty. 
then,  it  will the sold at price and subtotal. then it will show amount paid distributed across the payment methods: these 5 coloumns: cash - bKash - bank - card - installments.

### order change restock
Go through all the code for order creation and update.
when an order is update from orders page (before confirmation), we simply update the reserved_products table.
and after delivery, we accept return-exchange.
but, whithin that phase, for fulfilled/scanned/confirmed orders, when they are updated (they need to be updated, so if there was some code that prohibits that, update that),
when, an order is editted to add/remove products, when an already scanned product qty is deducted, instead of being deducting by a counter (like we did before scanning), we should be shown a list of barcodes, we click to remove scanned barcodes from the order.
the removed barcodes should be made available again. reserved_product table should be updated. 

### product list
update the product list page.
for each product variant, beside the tags: tota;, online and offline, we need an extra tag: "reserved"
in the reserved tag, we will write the total reserved qty for the product.
N.B.:do not change any other logic or code. simply write the reserved qty of that variant from the reserved_product table there.

### product list sorting
we want this update: 
currently, in the product list the prroduct variations come sorted from backend following some logic.
update it so that, under a product sku, the variants come sorted in descending order of total inventory.

update the query builder to handle this sorting efficiently.

### variation_suffix gone 
there was a frontend hardcode where we slugified the variation suffix ("- Red Purple - xl"  became "-red-purple-xl").
this needs to be fixed. Variation suffix must maintain whatever form it was given as.  


### service_only order type.
add a new order type: "service_only".
in social commerce page, when we create an

### product stock up and down 
the order editing is a bit incosnsistent:
when we edit an order (implies social commerce and ecom order) that has not been scanned, confirmed or delivered:
we should be able to increase or decrease qty of any product in the list on top of existing behaviour.
when we edit an order that has been scanned or confirmed (scanned and confirmed may mean the same thing):
decreasing qty will not use the counter (deactivate it for scanned products).
instead, for removal, we should be shown a list of barcodes. we click to remove scanned barcodes from the order.
removed barcodes should be made available again. reserved_product table should be updated.
increasing qty will simply use the counter. we'll scan them later from the social-commerce/package page.no choosing barcode.
we can also add new products to the list (increasing and decreasing their qty will simply use the counter), no choosing barcode.
the order status should be made pending when we add newer products (and stay confirmed when we did nothing but decreased qty of a product)
this partially scanned order will be scanned from social-commerce/package page later. (the process must be robust) (robust means, the page will be aware of the partial scanned, i.e, 3 is already scanned, 2 is left) (the packing page most likely already supports this).

pls make this. pls.

### edit order prev payment
When we are editing an order with some previous payment (partial, or full) is not preserved.
An example:
an order of total 30, we paid 20 in advance. When editing that order, 20 advance is not saved/shown.
an order of total 30, we paid 30 in advance. When editing that order, 30 advance is not saved/shown.

pls make it. pls fix this.


### service only order 

social_commerce page already handles service only orders.
we need a new order_status: service_only.
we'll need a new "service_only' filter in orders page.

the last_updated filter is not  working properly in orders page.
we created an order  on May 15, packed  it (confirmed) on May 16. 
so, when we select may 16 and date based on Last_updated, we should be able to see it.
This may be almost working. we see a flash of that order,  but, some final code isforcefully  hiding it.

### store-assignment
update the socia-commerce orders workflow. currently, when we place a social-commerce order it automatically get's assigned to the first online store.
update the entire workflow to match this:
social-commerce orders will not get automatically assigned to any store. instead, they'll have the status pending_assignment and simply be created without any stores assigned.
for e-commerce orders, they'll also be pending_assignment and be created without a store assigned.
then, add the "store assignment" option in the admin side sidebar and that will redirect to the /store-assignment page.
that page shoulbe already be working, but double to check to make sure it calls all the backend routes perfectly with meaning.
from there we can assign stores to orders and there status will be "assigned_to_store". 
store-assignemnt page will only show "pending_assignment" orders and a filter for oldest_first and newest_first.
we are not creating a new workflow. store_assignment logic already existed, we simply had a bypass for that till now. we simply want to use that feature again now.
so, when in doubt, simply look at the store-assignemnt workflow to infer what to do.

we also want you to check these:
if new stores can be created.
one store from all stores can be selected when creating Purchase-orders, in /pos page, in returnc exchange modals in /lookup page.

after you're done, write me a "May_27_deshio_store_assignment.md" file that will discuss your main workplan, what files you changed, what you changed, and why you changed. in the end, what we achieved.

### ECm cat filter 
in the e-commerce side, in the e-commerce/products page, there's a sidebar for category selection, price etc filtering.
the backend route this specific page calls isn't properly filtering based on the category (it uses category id as parameter).
what we saw was, within a particular category, selecting it, or any of it's subcategories always returned one same product while many products existed under various subcategories.

what i want you to do is, for only the backend grouping function this filtering on this e-commerce/product page calls, make the category wise filtering robust and properly functional.

then, write me "May_27_deshio_ecommerce_category_filtering.md" file that will discuss your main workplan, what files you changed, what you changed, and why you changed. in the end, what we achieved.


### PO deletion
we want a purchase order deletion feature in deshio. see the entire codebase in errum (it already has a fully functional purchase order deletion feature from /purchase-order page).

but, the purchase-order deletion will have this feature:
purchase order deletion follows this serial: all product_batch will be deleted, then the purhase order will be deleted.
we will create a new pivot table to store deleted_purchase_order_id <-> barcode. so, when already sold barcode is being returned/exchanged in lookup page, or we search a barcode in barcode_history in lookup page, when we don't find the PO and batch for the barcode (since we deleted them), we'll simply show "PO deleted" "Batch deleted". this barcode should be returnable and exchangeable.
however, we can't relable barcode with a deleted_purchase_order_id. however, when returned/exchanged, they should be removed from the pivot table and then we can relabel them.
make this whole process extremely robust and working.


### image gallery viewer in product/id page.
add these features in e-commerce/product/id page:
when an sku has more than 15 variaitons, wrap the variation cards in a dropdown.
above the dropdown, show a single variation card showing the current selected variation.

our actual e-commerce has upto 330 variations under single sku. so, the variation cards get a lot bloated.
in this mode, we want all the images of all the variations in that sku in an infinite sidewise scroll. whichever image is on display, it's corresponding variation card will also be highlighted/active in the dropdown. and abaove it.
but, to avoid network load and cach issues, in this mode, at a time, only limited number of images will be loaded in the image scroll, the images of variant choosen, the 3 variants before and 3 variants after. when out of scope, delete the cached images to load the new ones.
the pre-existing behaivor of clicking a variation card and image going to that one should still exist.
pls make this.

### image issue:
we want you to check the entire codebase for deshio.
the collections system (see /collections in admin side and e-commerce/collections/[slug] pg in the frontend and their backend code). 
the homepage settings page in the admin side 


### tester.

this is our latest codebase. 
go through the entire codebase and see in how many possible ways can this codebase be used and possible abused (to find out edge-cases).
then write me 7-8 ParticularTopicUseCases.md files that will have proper guidline that a professional software QA can follow as a guidline to test all the possible paths.
we are expecting rules like (roughly):
create 3 category

create a product with one of them as category. then archive product from product/list

then, archive the product (for mproduct/list). then this cat too will be deleteable

delete 2 categories. (with product shouldn’t be deleteable. without should be)

in third one, make a subcategory. then try deleting the mother category (shouldn’t be possible)

delete the subcategory (should be possible) then delete the upper one (should be now possible)

Multi-store 

create a new Store “Bashundhara” and keep it “offline”

create PO, approve and receive it in said store.

**step 3**: check product batch page for qty and location (store name likha thake)

**step 4**: check view inventory page for “offline” qty. 

do a few POS orders from bashundhara

**step 5**: repeat step 3 and step 4

return exchange POS orders (of bashundhara) to both Bashundhara and Office.

step 5

resell these barcodes from bashundhara (the ones that were returned to bashundhara) 

do a few e-commerce orders

do a few social-commerce orders 

step 5 (product should be reserved)

check if they (both ecm and scm) already get assigned to Office (shoudn’t be) (check in orders page. status should be pending_assignment, means not assigned)

then, assign them to Bashundhara (process simillar to errum) (store assignment page. also check if the recommendation is working on that page) (check oldest first-newest first filter)

then check orders page, order_status should be pending.

then pack them from online order packing.

step 5 (reserve released and offline and total qty will go down)

return exchange them in variation

resell them (in pos or online-order packing)

the useCases files should find absolutely all possible weird combos to find all typical happy path alongside the edge cases.

for example, create PO, receive partially, check inventoty, sell some products, return some, exchange some, then resell them, then return them. 
or let's say, create po, receive it, sell some product, delete PO, accept return.
etc.

these 7-8 useCases files will be on topics like Products, orders, Return_exchanges, hrm, reporting, etc etc.
a key point to note is, there are some backend code (at time simillar codes exist in multiple places), but only one is being currently used.
for example, return-echange can only be done from lookup page, all return-refund buttons have been hidden from /retturns page.

pls make these.




I've given you a zip of a formal documentation that our Software QA follows to test the entire Deshio workflow. We now want to automate this process. so, we want you to write a new Controller named FullTestingController. our aim is to test the backend code the frontend uses, exactly how a software QA would be doing. so, this controller will simply use the existing backend code and not update or rewrite anything. this controller will have parts for each of these useCases.md and carry out the tests accordingly. we want a html file in the backend public/ folder. we can login there with gmail and password. after login, we run the tests. we are shown live updates of what's being tested and how much is done and how much is left. also, these tests should also properly test what should not be working. like, a barcode with po deleted should not be sellable, etc. we want the page UI to be like this, avoid terminal like interface. instead use card-like lists. we should be shown, for each cases, what was tested (a quick summary of the test), if it worked or failed. and expected outcome. like: tried to sell barcode with PO deleted. correctly showed that not possible. etc. also, make sure backend response doesn't get timedOut. pls make this properly with all scenarios tested out. two key points: we don't want the sendToPathao tests to be included in this testing panel.



also, if it gets an error, the testing should not be stopped. the card will simply show: Tried this, failed. go this error: "with a copy button to copy the error." . and for regular incorrect behavious, a full statement for what was tried and what was expected and what was recevied (with a copy button.)


### store-assignment 

fix the store-assignment page cart availbility check workflow. 
currently,when something is added to cart, it tries to go through all batches in the store to ensure store can fulfhill the order (when manual-assignment is on).
we need to improve this.
a simple fix will be removing this pre-emptive checking and when we place an order (from social-commerce page) with manual-assignment on, make a request to backend to see if this order can be assigned to that store (see backend of /store-assignment page). if it can't don't proceed to the next page. the frontend toast shown should be dumb enough for people to understand, "Store X can't fulfill this order, missing items a,b ."
